- name: run the role for cluster setup
  hosts:
    - manager
    - worker
  become: true
  gather_facts: true
  vars:
    ansible_ssh_user: ubuntu
    for_user: zhu
    ansible_ssh_host_key_checking: false
  roles:
    - setup-cluster