- name: configure docker daemon
  template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: 0644

- name: restart docker
  ansible.builtin.service:
    name: docker
    state: restarted
